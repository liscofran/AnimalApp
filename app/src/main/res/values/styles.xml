<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Stile per lo Spinner -->
    <style name="MySpinnerStyle" parent="Widget.AppCompat.Spinner.Underlined">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Definizione di dimensioni -->
    <dimen name="padding_small">8dp</dimen>
    <dimen name="padding_medium">16dp</dimen>
    <dimen name="text_small">14sp</dimen>
    <dimen name="text_medium">18sp</dimen>

    <style name="MyCalendarViewStyle">
        <item name="android:textColor">#FF0000</item>
        <item name="android:textSize">18sp</item>
        <!-- altri attributi di stile per personalizzare il testo della data -->
    </style>

</resources>